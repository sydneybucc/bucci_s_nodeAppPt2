<a href="/">
  <i class="fa-fa-home"></i>
</a>

<ul>
  <h1>Movie Details</h1>
    <li>
      <h4>{{ filmData.movies_title }}</h4>
        <iframe width="560" height="315" src="https://www.youtube.com/embed/{{ movies_trailer }}" frameborder="0" allow="autoplay; encrypted-media" allowfullscreen></iframe>
      <p>{{ filmData.movies_runtime }}</p>
      <p>{{ filmData.movies_storyline }}</p>
      <p>{{ filmData.movies_year }}</p>
    </li>
</ul>

