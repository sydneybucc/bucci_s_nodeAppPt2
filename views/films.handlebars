<a href="/">
  <i class="fa-fa-home"></i>
</a>

<h1>{{ message }}</h1>

<ul>
  {{#each filmData}}
    <li>
      <h4>{{ movies_title }}</h4>
      <img src="images/{{ movies_cover }}" alt="Movie Trailer Cover">
      <a href="/film/{{movies_id}}" class="button">View Details</a>
    </li>
  {{/each}}
</ul>